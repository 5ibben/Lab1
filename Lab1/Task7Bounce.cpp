#include <iostream>
using namespace std;

/*
int main()
{
	//program prints all numbers between 0 and n, first in descending and then in ascending order.

	cout << "Enter a natural number: ";
	int n;
	cin >> n;

	if (n < 0)
	{cout << "Error! Input cannot be negative.";	}
	else
	{
		int increment = -1;
		int i = n;
		while (i <= n)
		{
			cout << i;
			if (i==0)	//when zero is reached, i starts incrementing instead of decrementing 
			{
				increment = 1;
			}
			i += increment;
		}
	}

	return 0;
}
*/
